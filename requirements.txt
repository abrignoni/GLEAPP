# 10.2 is the first release whose macOS arm64 wheel bundles libjpeg-turbo 3.0.1.
# The 3.0.0 build in 10.0.x and 10.1.0 has no SIMD and its C Huffman encoder
# flushes on an uninitialised flag, so a JPEG comes out corrupt at random
# (measured, see .claude/rules/gleapp-cross-platform.md).
Pillow>=10.2
pillow-heif>=0.13
texture2ddecoder>=1.0
# pyliblzfse for Windows on x64, vendored so Windows never needs a C compiler.
# PyPI publishes wheels for cp310 to cp313 only and none for cp314; the cp314
# wheel comes from iLEAPP, which built it for the same reason. The markers pin
# the machine as well as the OS: these are win_amd64, and on Windows-on-ARM pip
# would otherwise try to install them and fail. Anything the markers do not
# match compiles from the sdist on the line after them.
whl_files/pyliblzfse-0.4.1-cp310-cp310-win_amd64.whl; python_version == "3.10" and platform_system == "Windows" and platform_machine == "AMD64"
whl_files/pyliblzfse-0.4.1-cp311-cp311-win_amd64.whl; python_version == "3.11" and platform_system == "Windows" and platform_machine == "AMD64"
whl_files/pyliblzfse-0.4.1-cp312-cp312-win_amd64.whl; python_version == "3.12" and platform_system == "Windows" and platform_machine == "AMD64"
whl_files/pyliblzfse-0.4.1-cp313-cp313-win_amd64.whl; python_version == "3.13" and platform_system == "Windows" and platform_machine == "AMD64"
whl_files/pyliblzfse-0.4.1-cp314-cp314-win_amd64.whl; python_version == "3.14" and platform_system == "Windows" and platform_machine == "AMD64"
pyliblzfse>=0.4.1
zstandard>=0.22
# 7-Zip archive reading (a .7z found inside a source is expanded like a .zip).
# Pure-Python itself; its codec extensions (pyppmd, pybcj, inflate64, brotli,
# pycryptodomex) publish wheels for CPython 3.10-3.13 on Windows/macOS/Linux and
# build from the sdist otherwise, the same position as pyliblzfse.
py7zr>=0.21
imagehash>=4.3
numpy>=1.24
opencv-python-headless>=4.8
Flask>=3.0
tzdata>=2024.1
# The native desktop window (gleappGUI.py, `gleapp desktop`). The package is
# pywebview and the module it installs is `webview`; PyPI's package named `webview`
# is a different project with no wheels, so never install that one. pip brings
# the platform bindings with it, pythonnet on Windows and PyObjC on macOS, all as
# wheels for CPython 3.10 to 3.14 (PyPI metadata, 2026-09-15). One dependency,
# proxy_tools, is pure Python and published only as a source archive, so pip builds
# it with no compiler. Linux needs a GUI toolkit on top: README, "Desktop app".
pywebview>=5.0
