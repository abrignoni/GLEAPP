# Agent guidance

The project's guidance for any coding agent lives in `CLAUDE.md` and `.claude/rules/`.
The file names follow the other LEAPP repositories; the content applies whatever tool
reads it. Start with `CLAUDE.md`.
